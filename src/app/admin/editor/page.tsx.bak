import { Suspense } from 'react'
import EditorContent from './EditorContent'

export default function EditorPage() {
  return (
    <Suspense fallback={<div style={{background:'#F5F0E8',minHeight:'100vh',display:'flex',alignItems:'center',justifyContent:'center',fontFamily:'monospace',color:'#9A9895'}}>Loading...</div>}>
      <EditorContent />
    </Suspense>
  )
}
